# Add Repository with Submodule - Python Dependencies

# No external dependencies required - uses only Python standard library
# The script relies on:
# - GitHub CLI (gh) being installed on the system
# - Git being installed on the system
# - repo-builder package (in parent directory)
