# Local ruff configuration for subtree-converter

[lint]
# Extend the parent configuration but allow print statements for CLI output
ignore = [
    "T201",   # Allow print statements in CLI tool
    "N999",   # Allow hyphenated module name (subtree-converter)
]

[lint.per-file-ignores]
# Test files don't need type annotations and use assert
"test_*.py" = ["S101", "ANN", "PLR2004"]
