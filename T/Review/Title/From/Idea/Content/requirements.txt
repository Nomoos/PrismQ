# Test dependencies for T.Review.Title.From.Idea.Content
pytest>=7.0.0
pytest-cov>=4.0.0

# Runtime dependencies
requests>=2.28.0  # For Ollama API calls

# Required for src module (Config, database connections)
python-dotenv>=1.0.0
