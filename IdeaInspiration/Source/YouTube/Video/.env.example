# Working Directory (automatically managed)
WORKING_DIRECTORY=

# Database Configuration
# Use DATABASE_URL for database connection (SQLite or other databases)
# Examples:
#   SQLite: sqlite:///db.s3db (relative to working directory)
#   SQLite: sqlite:////absolute/path/to/db.s3db
#   PostgreSQL: postgresql://user:password@localhost/dbname
DATABASE_URL=sqlite:///db.s3db

# YouTube API Configuration (for search-based scraping)
YOUTUBE_API_KEY=your_youtube_api_key_here

# YouTube API Quota Management
# Daily quota limit in units (default: 10000)
# You can increase this if you have a higher quota from Google
YOUTUBE_DAILY_QUOTA_LIMIT=10000

# Path to quota tracking storage file (JSON format, not database)
# Note: SQLite databases are reserved exclusively for IdeaInspiration model
YOUTUBE_QUOTA_STORAGE_PATH=data/youtube_quota.json

# YouTube Channel Configuration (for channel-based scraping with yt-dlp)
# Example: https://www.youtube.com/@channelname or @channelname or UC1234567890
# Test channel: https://www.youtube.com/@SnappyStories_1
YOUTUBE_CHANNEL_URL=

# ==============================================================================
# NOTE: The following are MODULE RUNTIME PARAMETERS, not environment variables.
# They should be passed as CLI arguments using --top parameter.
# 
# Defaults are defined in src/core/config.py:
#   - YOUTUBE_MAX_RESULTS: 50
#   - YOUTUBE_CHANNEL_MAX_SHORTS: 10
#   - YOUTUBE_TRENDING_MAX_SHORTS: 10
#   - YOUTUBE_KEYWORD_MAX_SHORTS: 10
#
# Usage examples:
#   python -m src.cli scrape-channel --top 20
#   python -m src.cli scrape-trending --top 15
#   python -m src.cli scrape-keyword --keyword "ideas" --top 30
# ==============================================================================
